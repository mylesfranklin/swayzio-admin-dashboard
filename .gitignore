# dependencies
node_modules

# env / secrets
.env
.env.*
!.env.example

# build output
dist
.next
out
server/public

# typescript / next generated
*.tsbuildinfo
next-env.d.ts

# vercel / turbopack
.vercel
.turbo

# local agent/harness state
.local

# auto-installed agent skills (reproducible via skills-lock.json + `npx skills add`; symlink-heavy).
# skills-lock.json IS committed (the manifest); the installed/symlinked copies are not.
.agents/skills
.claude/skills
.devin/
.factory/
.goose/
.hermes/
.kiro/
.windsurf/

# misc
.DS_Store
vite.config.ts.*
*.tar.gz

# E2E harness secrets/artifacts (swayzio-admin-e2e)
.e2e/

# eve agent build artifacts
.eve/
.output/
